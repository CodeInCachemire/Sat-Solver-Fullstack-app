# Main SAT Solver frontend (satsolver.ahmadq.me)
server {
    listen 80;
    server_name satsolver.ahmadq.me localhost ~^satsolver\.; 

    # Serve frontend static files from /frontend directory
    location / {
        root /usr/share/nginx/html/frontend;
        try_files $uri $uri/ /index.html;
        index index.html index.htm;
    }

    # Proxy API requests to backend container
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health check endpoint for nginx
    location /nginx-health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

# Sudoku Solver frontend (sudokusolver.ahmadq.me)
server {
    listen 80;
    server_name sudokusolver.ahmadq.me ~^sudokusolver\.; 

    # Serve sudoku frontend static files from /sudoku_frontend directory
    location / {
        root /usr/share/nginx/html/sudoku_frontend;
        try_files $uri $uri/ /index.html;
        index index.html index.htm;
    }

    # Proxy API requests to backend container
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health check endpoint for nginx
    location /nginx-health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
