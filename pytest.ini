[pytest]
markers =
    unit: Unit tests using mocks only — no external dependencies
    api: API tests using FastAPI TestClient — no external dependencies
    integration: Require real Redis and/or PostgreSQL
    system: Require full Docker Compose stack running
timeout = 30
testpaths = comprehensive_tests
addopts = -v --tb=short
pythonpath = .
